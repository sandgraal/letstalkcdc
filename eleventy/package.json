{
  "name": "letstalkcdc-site",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "build": "eleventy --config=eleventy.config.cjs",
    "serve": "eleventy --config=eleventy.config.cjs --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "rimraf": "^5.0.5"
  }
}
