---
layout: null
permalink: 404.html
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Page not found — CDC: The Missing Manual</title>
    <meta content="width=device-width, initial-scale=1" name="viewport" />
    <meta content="noindex" name="robots" />
    <style>
      :root {
        --border: #2a3441;
        --muted: #9aa7b7;
        --surface: #0f1726;
        --bg: #0b121d;
      }
      html,
      body {
        height: 100%;
      }
      body {
        margin: 0;
        background: var(--bg);
        color: #e6eef7;
        font: 16px/1.6 system-ui, -apple-system, Segoe UI, Roboto, Inter, Arial,
          sans-serif;
        display: grid;
        place-items: center;
      }
      main {
        max-width: 900px;
        width: 100%;
        padding: 2rem;
      }
      .box {
        border: 1px solid var(--border);
        border-radius: 0.75rem;
        background: var(--surface);
        padding: 1.25rem;
      }
      h1 {
        margin: 0.25rem 0 0.5rem;
        font-size: clamp(1.4rem, 4vw, 2rem);
      }
      p.muted {
        color: var(--muted);
        margin: 0.25rem 0 0.75rem;
      }
      .grid {
        display: grid;
        gap: 1rem;
        grid-template-columns: 1fr;
      }
      @media (min-width: 860px) {
        .grid {
          grid-template-columns: 1fr 1fr;
        }
      }
      .card {
        border: 1px solid var(--border);
        border-radius: 0.6rem;
        padding: 1rem;
        background: #0b1322;
      }
      .card h2 {
        margin: 0 0 0.35rem;
        font-size: 1.05rem;
      }
      .card p {
        margin: 0.2rem 0 0.6rem;
      }
      .links {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
        margin-top: 0.35rem;
      }
      .links a {
        border: 1px solid var(--border);
        border-radius: 0.5rem;
        padding: 0.35rem 0.6rem;
        text-decoration: none;
        color: inherit;
      }
      .links a:hover,
      .links a:focus {
        background: rgba(255, 255, 255, 0.06);
        outline: none;
      }
      .btnrow {
        display: flex;
        gap: 0.5rem;
        flex-wrap: wrap;
        margin-top: 1rem;
      }
      .btn {
        display: inline-block;
        border: 1px solid var(--border);
        border-radius: 0.5rem;
        padding: 0.55rem 0.75rem;
        text-decoration: none;
        color: inherit;
      }
      .btn:hover,
      .btn:focus {
        background: rgba(255, 255, 255, 0.06);
        outline: none;
      }
      code {
        font: 13px/1.45 ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
      }
      .small {
        font-size: 0.9rem;
        color: var(--muted);
      }
    </style>
    <link href="https://letstalkcdc.nfshost.com/404.html" rel="canonical" />
    <meta
      content="Hands-on, precise guidance for building and operating Change Data Capture pipelines."
      name="description"
    />
  </head>
  <body>
    <main>
      <div class="box">
        <h1>We couldn’t find that page</h1>
        <p class="muted">
          the link may be outdated or mistyped. here are quick paths to popular
          sections.
        </p>
        <div class="btnrow">
          <a class="btn" href="/">← there's no place like home</a>
          <a class="btn" href="/overview/">series overview</a>
        </div>
      </div>
    </main>
    <script>
      (function () {
        const p = (location.pathname || "").toLowerCase();
        const map = {
          "/quickstart-postgres": "/quickstarts/quickstart-postgres/",
          "/quickstart-mysql": "/quickstarts/quickstart-mysql/",
          "/quickstart-oracle": "/quickstarts/quickstart-oracle/",
          "/oracle": "/oracle-notes/",
          "/quickstarts": "/everything-else/"
        };
        if (map[p]) {
          location.replace(map[p]);
        }
      })();
    </script>
  </body>
</html>
