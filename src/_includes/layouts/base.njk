<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% if title %}{{ title }} | {{ site.title }}{% else %}{{ site.seoTitle }}{% endif %}</title>
    <meta name="description" content="{{ description | default(site.description) }}">
    <meta name="robots" content="index,follow,max-image-preview:large">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png">
    <link rel="icon" href="/favicon.svg" type="image/svg+xml">
    <link rel="canonical" href="{{ site.host }}{{ canonicalPath | default(page.url) }}">
    <meta name="theme-color" content="#0f172a">
    {% if head_extra %}{{ head_extra | safe }}{% endif %}
    <link rel="stylesheet" href="/assets/css/styles.css">
  </head>
  <body>
    <a class="skip-link" href="#main">Skip to content</a>
    <header class="global-header">
      <div class="nav-container">
        <a class="site-title" href="/">{{ site.seoTitle }}</a>
        <div class="nav-right">
          <nav aria-label="Primary" class="nav-links">
            <a class="{% if page.url == '/' %}active{% endif %}" href="/">Home</a>
            <a class="{% if page.url | startsWith('/overview') %}active{% endif %}" href="/overview/">The Series</a>
          </nav>
          <button aria-label="Toggle dark mode" class="theme-toggle" data-toggle-theme type="button">🌓</button>
        </div>
      </div>
    </header>

    <main id="main">{{ content | safe }}</main>

    <script type="module" src="/assets/js/app.js"></script>
    {% if scripts %}
      {% for script in scripts %}
        <script type="module" src="{{ script }}"></script>
      {% endfor %}
    {% endif %}
    <footer class="site-footer">
      <p>{{ site.copyright }}</p>
    </footer>
  </body>
</html>
