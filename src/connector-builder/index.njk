---
layout: base.njk
title: "Connector Config Builder — Debezium (Postgres • MySQL • Oracle)"
description: "Generate Debezium connector configs with the right settings for DLQ, snapshot modes, and filtering."
canonicalPath: "/connector-builder/"
head_extra: |
  <link rel="stylesheet" href="{{ '/assets/css/pages/connector-builder.css' | url }}">
scripts:
  - "/assets/js/pages/connector-builder.js"
---
{% import "components/ui.njk" as ui %}

{{ ui.hero(heroConfig) | safe }}

<div class="builder-app">
<div class="builder-app__back"><a href="/overview/#series-connector-builder">← Back to Overview</a></div>
  <div class="builder-app__content">
    <div class="wrap">
      <section aria-labelledby="builder-inputs" class="panel">
        <h2 id="builder-inputs">1) choose source &amp; version</h2>
        <div aria-label="source" class="tabs" role="tablist">
          <button class="active" data-src="postgres" role="tab">postgres</button>
          <button data-src="mysql" role="tab">mysql</button>
          <button data-src="oracle" role="tab">oracle</button>
        </div>
        <div class="row">
          <label for="debz">debezium version:</label>
          <select id="debz">
            <option value="2">2.x (topic.prefix)</option>
            <option value="1">1.x (database.server.name)</option>
          </select>
        </div>
        <h2 style="margin-top:0.6rem">2) connection &amp; naming</h2>
        <div class="two">
          <div>
            <div class="row"><label for="cname">connector name:</label><input id="cname" type="text" value="inventory-connector"></div>
            <div class="row"><label for="curl">connect url:</label><input id="curl" type="text" value="http://localhost:8083"></div>
            <div class="row"><label for="dbname">database:</label><input id="dbname" type="text" value="inventory"></div>
          </div>
          <div>
            <div class="row"><label for="user">username:</label><input id="user" type="text" value="inventory"></div>
            <div class="row"><label for="pass">password:</label><input id="pass" type="password" value="inventory"></div>
            <div class="row"><label for="dlq">dlq topic (optional):</label><input id="dlq" type="text" value="dlq.inventory"></div>
          </div>
        </div>

        <h2>3) filters &amp; snapshot</h2>
        <details open>
          <summary>table filtering</summary>
          <div class="row"><label for="include">include regex:</label><input id="include" type="text" placeholder="inventory.public.customers"></div>
          <div class="row"><label for="exclude">exclude regex:</label><input id="exclude" type="text" placeholder="inventory.public.audit_.*"></div>
        </details>
        <details open>
          <summary>snapshot options</summary>
          <div class="row">
            <label for="snapshot">snapshot mode:</label>
            <select id="snapshot">
              <option value="initial">initial</option>
              <option value="initial_only">initial_only</option>
              <option value="never">never</option>
            </select>
          </div>
          <div class="row"><label for="fetch">fetch size:</label><input id="fetch" type="text" value="2000"></div>
        </details>
        <details>
          <summary>advanced settings</summary>
          <div class="hint">
            leave defaults alone unless you know the operational impact. overrides are preserved below.
          </div>
          <div class="row"><label for="taskMax">tasks.max:</label><input id="taskMax" type="text" value="1"></div>
          <div class="row"><label for="heartbeat">heartbeat interval ms:</label><input id="heartbeat" type="text" value="30000"></div>
        </details>
      </section>

      <aside class="panel right" aria-labelledby="builder-preview">
        <h2 id="builder-preview">4) preview &amp; apply</h2>
        <button class="btn copy" id="copyConfig">copy</button>
        <pre><code id="config"></code></pre>
        <details>
          <summary>curl to Kafka Connect</summary>
          <button class="btn copy" id="copyCurl">copy</button>
          <pre><code id="curlCmd"></code></pre>
        </details>
        <section class="hint">
          <strong>Remember:</strong> apply config changes with <code>PUT /connectors/&lt;name&gt;/config</code> to avoid task bounce.
        </section>
      </aside>
    </div>
  </div>
</div>
