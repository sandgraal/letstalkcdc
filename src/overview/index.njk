---
layout: base.njk
title: "Series Overview"
description: "Series hub for advanced CDC patterns. Exactly-once semantics, multi-tenancy, partitioning, and reconciliation."
canonicalPath: "/overview/"
head_extra: |
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "ItemList",
      "name": "CDC Learning Series",
      "itemListElement": [
        {"@type": "ListItem", "position": 1, "name": "Interactive Introduction to CDC", "url": "{{ site.host }}/intro/"},
        {"@type": "ListItem", "position": 2, "name": "Exactly-Once Semantics", "url": "{{ site.host }}/exactly-once/"},
        {"@type": "ListItem", "position": 3, "name": "Multi-Tenancy: Cost vs Isolation", "url": "{{ site.host }}/multi-tenancy/"},
        {"@type": "ListItem", "position": 4, "name": "Hands-On Lab: Kafka + Debezium + Postgres", "url": "{{ site.host }}/lab-kafka-debezium/"}
      ]
    }
  </script>
---
{% import "components/ui.njk" as ui %}

{# Debug hero config #}
{# {{ heroConfig | dump }} #}
{{ ui.hero(heroConfig) | safe }}

<section class="series-card-grid">
  {% for card in seriesCards %}
    {{ ui.series_card(card) | safe }}
  {% endfor %}
</section>
