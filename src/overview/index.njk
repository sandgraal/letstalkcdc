---
layout: base.njk
title: "Series Overview"
description: "Series hub for advanced CDC patterns. Exactly-once semantics, multi-tenancy, partitioning, and reconciliation."
canonicalPath: "/overview/"
head_extra: |
  <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "ItemList",
      "name": "CDC Learning Series",
      "itemListElement": [
        {"@type": "ListItem", "position": 1, "name": "Interactive Introduction to CDC", "url": "{{ '/intro/' | url | absoluteUrl(site) }}"},
        {"@type": "ListItem", "position": 2, "name": "Exactly-Once Semantics", "url": "{{ '/exactly-once/' | url | absoluteUrl(site) }}"},
        {"@type": "ListItem", "position": 3, "name": "Multi-Tenancy: Cost vs Isolation", "url": "{{ '/multi-tenancy/' | url | absoluteUrl(site) }}"},
        {"@type": "ListItem", "position": 4, "name": "Hands-On Lab: Kafka + Debezium + Postgres", "url": "{{ '/lab-kafka-debezium/' | url | absoluteUrl(site) }}"}
      ]
    }
  </script>
---
{% import "components/ui.njk" as ui %}

{# Debug hero config #}
{# {{ heroConfig | dump }} #}
{{ ui.hero(heroConfig) | safe }}

<section class="series-card-grid">
  {% for card in seriesCards %}
    {{ ui.series_card(card) | safe }}
  {% endfor %}
</section>
